export { default as WeekSlider } from './WeekSlider';

// Export types for consumers
export type { WeekSelection, WeeklySliderProps } from './types';

// Export utilities if needed by consumers
export { getStartOfWeek, createWeekSelection } from './utils';

// Export theme for customization
export { theme } from './styles';
